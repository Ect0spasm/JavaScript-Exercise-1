for (let num= "#"; num < "########"; num += "#")
console.log(num)





function makeboard(width,height){
    let board = "";
    for(y = 0; y < height; y++){
        (y+x) % 2 === 0 ? board
    :
    board += "/n";
}
return board;

console.log(triangle(7));}